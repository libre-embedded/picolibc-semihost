# =====================================
# generator=datazen
# version=3.2.3
# hash=73d2097061fdc7ff7f090f00eed7be20
# =====================================
---
project:
  name: picolibc-semihost

  github: {owner: &self libre-embedded}
  version: {major: 0, minor: 3, patch: 0}
variants:
  clang:
    suffix: &clang_version "-19"
    enabled: false

  # Disable regular targets.
  debug:
    enabled: false
  opt:
    enabled: false
  clang-opt:
    enabled: false

includes:
  - local/yambs.yaml
