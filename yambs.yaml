# =====================================
# generator=datazen
# version=3.2.3
# hash=419bfb945e50db283bc7c3000138020d
# =====================================
---
project:
  name: picolibc-semihost

  github: {owner: &self vkottler}
  version: {major: 0, minor: 2, patch: 4}
variants:
  clang:
    suffix: &clang_version "-19"
    enabled: false

  # Disable regular targets.
  debug:
    enabled: false
  opt:
    enabled: false
  clang-opt:
    enabled: false

includes:
  - local/yambs.yaml
