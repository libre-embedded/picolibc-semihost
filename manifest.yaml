---
default_target: groups-all

params:
  slug: picolibc_semihost

includes:
  - config/includes/license.yaml
  - config/includes/funding.yaml
  - config/includes/yambs.yaml

compiles:
  - name: local
    configs:
      - local/configs

groups:
  - name: all
    dependencies:
      - groups-license
      - groups-funding
      - groups-yambs-native-renders
      - groups-yambs-embedded-renders
